<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itheima.mapper.SetmealDishMapper">
    <insert id="insertBatch">
        INSERT INTO setmeal_dish (setmeal_id, dish_id, name, price, copies)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.setmealId}, #{item.dishId}, #{item.name}, #{item.price}, #{item.copies})
        </foreach>
    </insert>

    <delete id="deleteBySetmealId">
        DELETE FROM setmeal_dish WHERE setmeal_id = #{setmealId}
    </delete>

    <delete id="deleteBySetmealIds">
        DELETE FROM setmeal_dish WHERE setmeal_id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>


    <select id="getSetMealIdsByDishIds" resultType="java.lang.Long">
        select setmeal_id FROM setmeal_dish
        WHERE dish_id IN
        <foreach collection="dishIds" item="dishId" open="(" separator="," close=")">
            #{dishId}
        </foreach>
    </select>

    <select id="getBySetmealId" resultType="com.itheima.pojo.entity.SetmealDish">
        SELECT * FROM setmeal_dish WHERE setmeal_id = #{setmealId}
    </select>
    <select id="getBySetmealIdWithStatus" resultType="com.itheima.pojo.entity.SetmealDish">
        SELECT sd.id, sd.setmeal_id, sd.dish_id, sd.name, sd.price, sd.copies, d.status
        FROM setmeal_dish sd
        JOIN dish d ON sd.dish_id = d.id
        WHERE sd.setmeal_id = #{setmealId}

    </select>

</mapper>
