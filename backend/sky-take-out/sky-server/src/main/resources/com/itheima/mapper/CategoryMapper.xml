<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itheima.mapper.CategoryMapper">

    <!-- 修改分类（动态更新属性） -->
    <update id="update" parameterType="com.itheima.pojo.entity.Category">
        UPDATE category
        <set>
            <if test="name != null"> name = #{name}, </if>
            <if test="type != null"> type = #{type}, </if>
            <if test="sort != null"> sort = #{sort}, </if>
            <if test="status != null"> status = #{status}, </if>
            <if test="updateTime != null"> update_time = #{updateTime}, </if>
            <if test="updateUser != null"> update_user = #{updateUser}, </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 分页查询 -->
    <select id="pageQuery" resultType="com.itheima.pojo.entity.Category" parameterType="com.itheima.pojo.dto.CategoryPageQueryDTO">
        SELECT *
        FROM category
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
        </where>
        ORDER BY sort ASC, update_time DESC
    </select>

    <select id="listByType" resultType="com.itheima.pojo.vo.CategoryVO">
        SELECT * FROM category
        where status = 1
            <if test="type != null">
                and type = #{type}
            </if>
        ORDER BY sort ASC, create_time DESC
    </select>
    <select id="list" resultType="com.itheima.pojo.entity.Category">
        SELECT * FROM category
        where status = 1
        <if test="type != null">
            and type = #{type}
        </if>
        ORDER BY sort ASC, create_time DESC
    </select>

    <!-- 根据id删除分类 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM category WHERE id = #{id}
    </delete>

</mapper>
